# Working Instructions

- Update the diary (see /diary skill) if adequate progress has been made since the last entry.
- Add tasks to the ticket / create new docmgr tickets as applicable to keep tickets at a decent size.
- Make sure to read the skills if you have forgotten how they work (/docmgr, /diary).
- Commit code and docs at appropriate intervals as you go.
- Save all downloaded files into the project (e.g. `data/`).
- Save scripts in the ticket's `scripts/` directory under `ttmp/.../scripts/`.
